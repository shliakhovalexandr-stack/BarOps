/* ============================================================
   BarOps — shared/config.js
   Єдине місце, де записана адреса бекенду.

   Досі вона була продубльована 45 разів у 38 файлах, білд-кроку немає (Netlify
   віддає ці файли сирими), тож підставити значення не було чим. Зміна домену
   бекенду означала правку кожного файлу окремо, а пропущений — одну мовчки
   зламану сторінку, яку помітять не одразу.

   Джерело істини — window.__BAROPS_API, який виставляє інлайн-скрипт у <head>
   ДО завантаження модулів. Літерал нижче лишається запобіжником: якщо цей
   модуль колись завантажать поза сторінкою застосунку, він не має падати.
   ============================================================ */

export const API_URL =
  (typeof window !== 'undefined' && window.__BAROPS_API) ||
  'https://barops-backend-production.up.railway.app';
